#include <stdio.h>#include <string.h>#include <stdlib.h>#include <limits.h>#include <math.h>#include "wofost.h"#include "extern.h"int GetMeteoData(char* inputfile){    char line[MAX_STRING];    int i, Station;    float A, B, Rad, Rainfall, VapourPressure;    FILE *fq;    /* Open file */    if ((fq = fopen(inputfile, "rt")) == NULL)    {        fprintf(stderr, "Cannot open meteo input file. %s\n", inputfile);         exit(0);    }    i = 0;    while(fgets(line, MAX_STRING, fq)) {        if(line[0] == '*' || line[0] == ' ' || line[0] == '\n'){            continue;        }                if(i == 0){            sscanf(line,"%f %f %f %f %f", &Longitude, &Latitude, &Altitude,  &A, &B);            AngstA = fabs(A);            AngstB = fabs(B);            i++;        }        else {            sscanf(line,"%d %d %d %f %f %f %f %f %f", &Station,                                                   &MeteoYear[i],                                                   &MeteoDay[i],                                                   &Rad,                                                  &Tmin[i],                                                   &Tmax[i],                                                   &VapourPressure,                                                  &Windspeed[i],                                                   &Rainfall);            /* Transform Radiation from KJ m-2 d-1 to J m-2 d-1 */            Radiation[i] = 1000.*Rad;            /* Transform Rain from mm d-1 to cm d-1 */            Rain[i] = 0.1 * Rainfall;            /* Transform Vapour from kPa to hPa */            Vapour[i] = 10. * VapourPressure;            i++;        }    }    fclose(fq);     return 1;}